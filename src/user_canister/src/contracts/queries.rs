use std::collections::HashMap;

use ic_cdk_macros::query;

use crate::USER_FILES;
use crate::files::FileNode;
use crate::user::User;

// #[query]
//
// fn x() -> String {
//     "contract".to_string()
// }
